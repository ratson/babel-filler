{
  "name": "babel-plugin-filler",
  "version": "0.0.0",
  "description": "Fill undeclared variables per configuration.",
  "scripts": {
    "lint": "eslint --ext js,md .",
    "test": "jest",
    "precommit": "lint-staged",
    "prepush": "jest"
  },
  "dependencies": {},
  "devDependencies": {
    "eslint": "^4.19.1",
    "eslint-config-concise": "^0.20.0",
    "eslint-config-concise-jest": "^0.20.0",
    "husky": "^0.14.3",
    "jest": "^22.4.3",
    "lint-staged": "^7.0.0",
    "npm-run-all": "^4.1.2",
    "prettier-eslint-cli": "^4.7.1"
  },
  "repository": "ratson/babel-plugin-filler",
  "license": "MIT",
  "engines": {
    "node": ">=6"
  },
  "files": [
    "*.md",
    "index.js"
  ],
  "keywords": [],
  "lint-staged": {
    "*.js": [
      "prettier-eslint --write",
      "eslint --fix",
      "git add"
    ]
  }
}
